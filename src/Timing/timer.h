#ifndef TIMER_H
#define TIMER_H

#include <chrono>
#include <vector>
#include <iostream>

class Timer {
  public:
    Timer(std::string timer_name);

  private:
    std::string _name; 
    std::vector

};

#endif
